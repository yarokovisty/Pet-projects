package org.pet.project.rickandmorty.feature.character.impl.presentation.viewmodel

// Use cases are simple pure functions with no external dependencies.
// Tests use the real implementations directly since there's no need to mock them.
// The ViewModel is the unit under test, not the use cases.